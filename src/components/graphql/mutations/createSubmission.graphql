mutation CreateSubmission($options: CreateSubmissionInput!) {
	createSubmission(options: $options) {
		errors {
			field
			message
		}
		success {
			field
			message
		}
		submission {
			fileKey
		}
	}
}
